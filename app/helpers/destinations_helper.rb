module DestinationsHelper
    # def parse_json(destination)
    #     destination.location ? puts YAML.load(destination.location) : "No"
    #     return destination.location ? JSON.parse(destination.location) : "Not set yet"
    # end
end
